[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    auth: Authentication related tests
    tasks: Task CRUD related tests
    multi_user: Multi-user isolation tests
    integration: Integration tests
    unit: Unit tests