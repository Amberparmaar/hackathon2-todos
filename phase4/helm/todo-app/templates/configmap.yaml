apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
data:
  BACKEND_URL: "http://todo-backend:{{ .Values.backend.service.port }}"
  FRONTEND_URL: "http://todo-frontend:{{ .Values.frontend.service.port }}"
  LOG_LEVEL: "info"
